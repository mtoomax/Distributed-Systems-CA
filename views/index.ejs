<!DOCTYPE html>
<html>
  <head>
    <title>gRPC Calculator</title>
    <style>
      body {
        font-family: sans-serif;
        padding: 20px;
        background: #f8f8f8;
      }

      .calculator,
      .weather {
        max-width: 400px;
        margin: 30px auto;
        padding: 20px;
        background: #fff;
        border-radius: 8px;
        box-shadow: 0 0 10px #ccc;
      }

      input,
      select,
      button {
        padding: 10px;
        margin: 5px 0;
        width: 100%;
      }

      h2 {
        margin-top: 0;
      }

      .logout-container {
      position: absolute;
      top: 20px;
      right: 20px;
    }

    .logout-btn {
      background: #e74c3c;
      border: none;
      color: white;
      padding: 10px 16px;
      border-radius: 6px;
      cursor: pointer;
      font-weight: bold;
      width: auto;
    }
    </style>
  </head>

  <div style="text-align: right; margin-bottom: 20px;">
    <form action="/logout" method="GET">
      <button class="logout-btn">🚪 Logout</button>
    </form>
  </div>

  <body>
    <%
    const safeTemperature = typeof temperature !== "undefined" ? temperature :
    null;
    %>

    <!-- Weather Section -->
    <div class="weather">
      <h2>🌤 gRPC Weather</h2>
      <form action="/fetch-weather" method="POST">
        <input type="text" name="area" required
          placeholder="Enter area (e.g., Ballyfermot)" />
        <button type="submit">Get Temperature</button>
      </form>

      <% if (safeTemperature !== null) { %>
      <h3>🌡 Temperature: <%= safeTemperature %> °C</h3>
      <% } %>
    </div>
  </body>
</html>
